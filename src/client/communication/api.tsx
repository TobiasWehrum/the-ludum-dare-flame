//import axios from "axios";

/*
export function createSession() {
    return axios.post(`/api/sessions`).then(res => res.data as ISession);
}

export function getSession(sessionId: string) {
    return axios.get(`/api/sessions/${sessionId}`).then(res => res.data as ISession);
}

export function generateNewSessionName(sessionId: string) {
    return axios.get(`/api/sessions/${sessionId}/generateNewName`).then(res => res.data as ISession);
}
*/